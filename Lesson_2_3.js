"use strict";
let a = -20, b = 5;

if (a >= 0 && b >= 0) {
    let dif = a - b;
    console.log(dif);
} else if (a < 0 && b < 0) {
    let mult = a * b;
    console.log(mult);
} else {
    let sum = a + b;
    console.log(sum);
}